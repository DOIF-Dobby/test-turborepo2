import { CodeBlock } from '@/components/code-block'

import Default from './default'
import DefaultRaw from './default?raw'
import Direction from './direction'
import DirectionRaw from './direction?raw'
import Gap from './gap'
import GapRaw from './gap?raw'

# Frame

Frame 컴포넌트는 flex를 사용하기 쉽게 만든 컴포넌트입니다.

## Default

<CodeBlock preview={<Default />} code={DefaultRaw} />

## Direction

<CodeBlock preview={<Direction />} code={DirectionRaw} />

## Gap

<CodeBlock preview={<Gap />} code={GapRaw} />
