import { CodeBlock } from '@/components/code-block'

import Default from './default'
import DefaultRaw from './default?raw'
import Placement from './placement'
import PlacementRaw from './placement?raw'
import OnClickItem from './on-click-item'
import OnClickItemRaw from './on-click-item?raw'
import RadioItem from './radio-item'
import RadioItemRaw from './radio-item?raw'
import CheckboxItem from './checkbox-item'
import CheckboxItemRaw from './checkbox-item?raw'
import DisableItem from './disable-item'
import DisableItemRaw from './disable-item?raw'
import DisableAnimation from './disable-animation'
import DisableAnimationRaw from './disable-animation?raw'
import CloseOnEscape from './close-on-escape'
import CloseOnEscapeRaw from './close-on-escape?raw'
import CloseOnOutsideClick from './close-on-outside-click'
import CloseOnOutsideClickRaw from './close-on-outside-click?raw'
import SubMenu from './sub-menu'
import SubMenuRaw from './sub-menu?raw'
import Separator from './separator'
import SeparatorRaw from './separator?raw'
import Controlled from './controlled'
import ControlledRaw from './controlled?raw'

# Menu

## Default

<CodeBlock preview={<Default />} code={DefaultRaw} />

## Placement

<CodeBlock preview={<Placement />} code={PlacementRaw} />

## On Click Item

<CodeBlock preview={<OnClickItem />} code={OnClickItemRaw} />

## Radio Item

<CodeBlock preview={<RadioItem />} code={RadioItemRaw} />

## Checkbox Item

<CodeBlock preview={<CheckboxItem />} code={CheckboxItemRaw} />

## Disable Item

<CodeBlock preview={<DisableItem />} code={DisableItemRaw} />

## Controlled

<CodeBlock preview={<Controlled />} code={ControlledRaw} />

## Close On Escape

`closeOnEscape` 속성을 `false`로 설정하면 ESC 키를 눌러도 메뉴가 닫히지 않습니다.

<CodeBlock preview={<CloseOnEscape />} code={CloseOnEscapeRaw} />

## Close On Outside Click

`closeOnOutsideClick` 속성을 `false`로 설정하면 외부 클릭으로 메뉴가 닫히지 않습니다.

<CodeBlock preview={<CloseOnOutsideClick />} code={CloseOnOutsideClickRaw} />

## Sub Menu

<CodeBlock preview={<SubMenu />} code={SubMenuRaw} />

## Separator

<CodeBlock preview={<Separator />} code={SeparatorRaw} />

## Disable Animation

<CodeBlock preview={<DisableAnimation />} code={DisableAnimationRaw} />
