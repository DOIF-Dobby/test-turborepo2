import { CodeBlock } from '@/components/code-block'

import Default from './default'
import DefaultRaw from './default?raw'
import WithButton from './with-button'
import WithButtonRaw from './with-button?raw'
import Placement from './placement'
import PlacementRaw from './placement?raw'
import DelayDuration from './delay-duration'
import DelayDurationRaw from './delay-duration?raw'
import Controlled from './controlled'
import ControlledRaw from './controlled?raw'

# Tooltip

## Default

<CodeBlock preview={<Default />} code={DefaultRaw} />

## With Button

<CodeBlock preview={<WithButton />} code={WithButtonRaw} />

## Placement

<CodeBlock preview={<Placement />} code={PlacementRaw} />

## Delay Duration

<CodeBlock preview={<DelayDuration />} code={DelayDurationRaw} />

## Controlled

<CodeBlock preview={<Controlled />} code={ControlledRaw} />
