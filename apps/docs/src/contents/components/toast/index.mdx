import { CodeBlock } from '@/components/code-block'

import Default from './default'
import DefaultRaw from './default?raw'
import Placement from './placement'
import PlacementRaw from './placement?raw'
import ItemType from './item-type'
import ItemTypeRaw from './item-type?raw'
import Timeout from './timeout'
import TimeoutRaw from './timeout?raw'
import TitleDescription from './title-description'
import TitleDescriptionRaw from './title-description?raw'
import ProgrammaticallyClose from './programmatically-close'
import ProgrammaticallyCloseRaw from './programmatically-close?raw'
import ProgrammaticallyUpdate from './programmatically-update'
import ProgrammaticallyUpdateRaw from './programmatically-update?raw'
import CustomStyle from './custom-style'
import CustomStyleRaw from './custom-style?raw'

# Toast

## Default

<CodeBlock preview={<Default />} code={DefaultRaw} />

## Title & Description

<CodeBlock preview={<TitleDescription />} code={TitleDescriptionRaw} />

## Placement

<CodeBlock preview={<Placement />} code={PlacementRaw} />

## Item Type

<CodeBlock preview={<ItemType />} code={ItemTypeRaw} />

## Timeout

`timeout` 속성을 사용하여 toast의 자동으로 제거되는 시간을 설정할 수 있습니다. `0`을 설정하면 자동으로 제거되지 않습니다.

<CodeBlock preview={<Timeout />} code={TimeoutRaw} />

## Programmatically Close

<CodeBlock
  preview={<ProgrammaticallyClose />}
  code={ProgrammaticallyCloseRaw}
/>

## Programmatically Update

<CodeBlock
  preview={<ProgrammaticallyUpdate />}
  code={ProgrammaticallyUpdateRaw}
/>

## Custom Style

<CodeBlock preview={<CustomStyle />} code={CustomStyleRaw} />
