import { CodeBlock } from '@/components/code-block'

import Default from './default'
import DefaultRaw from './default?raw'
import Handle from './handle'
import HandleRaw from './handle?raw'
import WithButtonTrigger from './with-button-trigger'
import WithButtonTriggerRaw from './with-button-trigger?raw'
import Controlled from './controlled'
import ControlledRaw from './controlled?raw'
import Sizes from './sizes'
import SizesRaw from './sizes?raw'
import HideCloseButton from './hide-close-button'
import HideCloseButtonRaw from './hide-close-button?raw'
import CloseOnOutsideClick from './close-on-outside-click'
import CloseOnOutsideClickRaw from './close-on-outside-click?raw'
import CloseOnEscape from './close-on-escape'
import CloseOnEscapeRaw from './close-on-escape?raw'
import CloseOnBoth from './close-on-both'
import CloseOnBothRaw from './close-on-both?raw'
import LongContent from './long-content'
import LongContentRaw from './long-content?raw'
import DisableAnimation from './disable-animation'
import DisableAnimationRaw from './disable-animation?raw'

# Modal

## Default

<CodeBlock preview={<Default />} code={DefaultRaw} />

## With Button Trigger

<CodeBlock preview={<WithButtonTrigger />} code={WithButtonTriggerRaw} />

## Controlled

<CodeBlock preview={<Controlled />} code={ControlledRaw} />

## Sizes

<CodeBlock preview={<Sizes />} code={SizesRaw} />

## Handle

<CodeBlock preview={<Handle />} code={HandleRaw} />

## Hide Close Button

<CodeBlock preview={<HideCloseButton />} code={HideCloseButtonRaw} />

## Close On Escape

`closeOnEscape` 속성을 `false`로 설정하면 `ESC` 키를 눌러도 모달이 닫히지 않습니다.

<CodeBlock preview={<CloseOnEscape />} code={CloseOnEscapeRaw} />

## Close On Outside Click

`closeOnOutsideClick` 속성을 `false`로 설정하면 모달 외부를 클릭해도 모달이 닫히지 않습니다.

<CodeBlock preview={<CloseOnOutsideClick />} code={CloseOnOutsideClickRaw} />

## Close On Both

`closeOnEscape`와 `closeOnOutsideClick` 속성을 모두 `false`로 설정하면 모달이 닫히지 않습니다.

<CodeBlock preview={<CloseOnBoth />} code={CloseOnBothRaw} />

## Long Content

<CodeBlock preview={<LongContent />} code={LongContentRaw} />

## Disable Animation

<CodeBlock preview={<DisableAnimation />} code={DisableAnimationRaw} />
