import { CodeBlock } from '@/components/code-block'
import TanstackTableBasic from './tanstack-table-basic'
import TanstackTableBasicRaw from './tanstack-table-basic?raw'
import WithTableComponent from './with-table-component'
import WithTableComponentRaw from './with-table-component?raw'
import WithAppTable from './with-app-table'
import WithAppTableRaw from './with-app-table?raw'
import VirtualizedRows from './virtualized-rows'
import VirtualizedRowsRaw from './virtualized-rows?raw'

# Table

## Overview

> [@tanstack/react-table](https://tanstack.com/table/latest) 기반으로 폼을 구현합니다.

## Tanstack Table Basic

`@tanstack/react-table`을 이용한 기본적인 테이블 구현 예제입니다.

<CodeBlock preview={<TanstackTableBasic />} code={TanstackTableBasicRaw} />

## With Table Component

`@repo/ui`에 정의된 테이블 컴포넌트를 이용한 예제입니다.

<CodeBlock preview={<WithTableComponent />} code={WithTableComponentRaw} />

## With App Table

`@repo/table`에 정의된 테이블 컴포넌트를 이용한 예제입니다.

<CodeBlock preview={<WithAppTable />} code={WithAppTableRaw} />

## Virtualized Rows

`<AppTable />`은 기본적으로 가상화된 테이블을 구현합니다.

<CodeBlock preview={<VirtualizedRows />} code={VirtualizedRowsRaw} />
