import { CodeBlock } from '@/components/code-block'

import TanstackTableBasic from './tanstack-table-basic'
import TanstackTableBasicRaw from './tanstack-table-basic?raw'
import WithTableComponent from './with-table-component'
import WithTableComponentRaw from './with-table-component?raw'
import WithAppTable from './with-app-table'
import WithAppTableRaw from './with-app-table?raw'
import VirtualizedRows from './virtualized-rows'
import VirtualizedRowsRaw from './virtualized-rows?raw'
import Pagination from './pagination'
import PaginationRaw from './pagination?raw'
import PaginationControlled from './pagination-controlled'
import PaginationControlledRaw from './pagination-controlled?raw'
import PaginationWithRouter from './pagination-with-router'
import PaginationWithRouterRaw from './pagination-with-router?raw'
import PaginationWithRouterServer from './pagination-with-router-server'
import PaginationWithRouterServerRaw from './pagination-with-router-server?raw'
import PaginationUseUrlPagination from './pagination-use-url-pagination'
import PaginationUseUrlPaginationRaw from './pagination-use-url-pagination?raw'
import Sorting from './sorting'
import SortingRaw from './sorting?raw'
import SortingControlled from './sorting-controlled'
import SortingControlledRaw from './sorting-controlled?raw'
import SortingWithRouter from './sorting-with-router'
import SortingWithRouterRaw from './sorting-with-router?raw'
import SortingWithRouterServer from './sorting-with-router-server'
import SortingWithRouterServerRaw from './sorting-with-router-server?raw'
import SortingWithUrlSorting from './sorting-use-url-sorting'
import SortingWithUrlSortingRaw from './sorting-use-url-sorting?raw'

# Table

## Overview

> [@tanstack/react-table](https://tanstack.com/table/latest) 기반으로 폼을 구현합니다.

## Tanstack Table Basic

`@tanstack/react-table`을 이용한 기본적인 테이블 구현 예제입니다.

<CodeBlock preview={<TanstackTableBasic />} code={TanstackTableBasicRaw} />

## With Table Component

`@repo/ui`에 정의된 테이블 컴포넌트를 이용한 예제입니다.

<CodeBlock preview={<WithTableComponent />} code={WithTableComponentRaw} />

## With App Table

`@repo/table`에 정의된 테이블 컴포넌트를 이용한 예제입니다.

<CodeBlock preview={<WithAppTable />} code={WithAppTableRaw} />

## Virtualized Rows

`<AppTable />`은 기본적으로 가상화된 테이블을 구현합니다. 데이터가 많아도 성능에 영향을 미치지 않습니다.

<CodeBlock preview={<VirtualizedRows />} code={VirtualizedRowsRaw} />

## Pagination

`enablePagination` 속성을 `true`로 설정하면 페이징을 사용할 수 있습니다. `AppTablePagination` 컴포넌트를 이용하여 페이징을 구현할 수 있습니다. 이건 Client Side Pagination 입니다.

<CodeBlock preview={<Pagination />} code={PaginationRaw} />

## Pagination Controlled

pagination을 외부에서 관리하고 싶을 때 사용합니다. 예를 들어 Server Side Pagination을 구현할 때 사용합니다.

<CodeBlock preview={<PaginationControlled />} code={PaginationControlledRaw} />

## Pagination with Router

pangination을 URL의 쿼리 파라미터로 관리하는 예제입니다.

<CodeBlock preview={<PaginationWithRouter />} code={PaginationWithRouterRaw} />

## Pagination with Router Server

pangination을 URL의 쿼리 파라미터로 관리하는 예제입니다. 서버 사이드 페이징을 할 때도 동일합니다.

<CodeBlock
  preview={<PaginationWithRouterServer />}
  code={PaginationWithRouterServerRaw}
/>

## Pagination Use Url Pagination

router 방식을 계속 구현하기에는 중복되는 코드가 많으므로 `@repo/next-utils`에 있는 `useUrlPagination`을 통해 쉽게 구현할 수 있습니다.

<CodeBlock
  preview={<PaginationUseUrlPagination />}
  code={PaginationUseUrlPaginationRaw}
/>

## Sorting

`enablePagination` 속성을 `true`로 설정하면 정렬 기능을 사용할 수 있습니다. 이건 Client Side Sorting 입니다.

<CodeBlock preview={<Sorting />} code={SortingRaw} />

## Sorting Controlled

정렬을 외부에서 관리하고 싶을 때 사용합니다. 예를 들어 Server Side Sorting을 구현할 때 사용합니다.

<CodeBlock preview={<SortingControlled />} code={SortingControlledRaw} />

## Sorting with Router

정렬을 URL의 쿼리 파라미터로 관리하는 예제입니다.

<CodeBlock preview={<SortingWithRouter />} code={SortingWithRouterRaw} />

## Sorting with Router Server

정렬을 URL의 쿼리 파라미터로 관리하는 예제입니다. 서버 사이드 정렬을 할 때도 동일합니다.

<CodeBlock
  preview={<SortingWithRouterServer />}
  code={SortingWithRouterServerRaw}
/>

## Sorting Use Url Sorting

router 방식을 계속 구현하기에는 중복되는 코드가 많으므로 `@repo/next-utils`에 있는 `useUrlSorting`을 통해 쉽게 구현할 수 있습니다.

<CodeBlock
  preview={<SortingWithUrlSorting />}
  code={SortingWithUrlSortingRaw}
/>
